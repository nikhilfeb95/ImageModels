package imagemodel.colordensity;

/**
 * A class which extends the AbstractColorDensity class.
 */
public class ColorDensityImpl extends AbstractColorDensity {
  /**
   * The constructor for the ColorDensityImpl class.
   * 
   * @param colorSize The colorSize the image is to be reduced to.
   */
  public ColorDensityImpl(int colorSize) {
    super(colorSize);
  }
}
